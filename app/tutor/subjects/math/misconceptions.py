"""
Math misconception diagnosis — future home for:

- Error pattern matching (sign errors, dropped terms, etc.)
- Structured error taxonomy (misapplied_chain_rule, sign_error, ...)
- Targeted remediation selection

Currently returns empty — architecture is reserved.
"""

MISCONCEPTION_PATTERNS: dict[str, dict] = {}
